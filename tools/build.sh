#!/bin/bash

# =============================================================================
# BUILD SCRIPT WRAPPER
# =============================================================================
# Простая обертка для перенаправления на новый путь

echo "🔄 Перенаправление в tools/build_src/build.sh"
echo

# Перенаправляем на новый скрипт
exec "$(dirname "$0")/build_src/build.sh" "$@" 