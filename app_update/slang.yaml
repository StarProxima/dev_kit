# Config
base_locale: en # базовый язык
fallback_strategy: base_locale # в случае ошибки возвращаемся к базовой локали

input_directory: lib/src/localizer/translations/locales # путь хранения переводов
output_directory: lib/src/localizer/translations/gen # путь генерации переводов
input_file_pattern: .yaml
output_file_name: translations.g.dart
translate_var: translationss
# output_format: multiple_files


# imports:
#   - '../../core.dart'
  
# contexts:
#   Reasons:
#     generate_enum: false # turn off enum generation


# string_interpolation: braces # в json используем так: "Наш параметр {параметр}"

# enumName: AppLocale  # название enum локали

# key_case: camel # именование переменных в соответствии со спецификацией dart
# key_map_case: null
# param_case: camel

# Для возможности загружать переводы с сервера
translation_overrides: false
flat_map: false

namespaces: false
flutter_integration: false

# locale_handling: false # remove unused t variable, LocaleSettings, etc.
translation_class_visibility: public


