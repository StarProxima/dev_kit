name: app_update
description: App updates with the possibility of deprecation, required updates, various app stores and custom UI
version: 0.0.3
homepage: https://github.com/StarProxima/dev_kit/tree/develop/app_update
screenshots:
 - description: 'App Update Package Thumbnail'
   path: assets/package_thumbnail.png

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  yaml: ^3.1.2
  package_info_plus: ^8.3.0
  pub_semver: ^2.2.0
  html: ^0.15.4
  shared_preferences: ^2.3.0
  url_launcher: ^6.1.14
  store_checker: ^1.8.0
  collection: ^1.18.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^3.0.0

  mocktail: ^1.0.0

  project_kit:
    path: ../unpublished/project_kit
    
  clock: ^1.1.1
  path: ^1.8.0
  test: ^1.25.15

