settings_0:
  title: 
    en: |-
      ## New version for ($appName)[https://example.com]
      ### Version $releaseVersion is available!
    es: La versión $releaseVersion está disponible!
    ru: Доступна новая версия!

  description: |-
    A new version of $appName is available!
    Version $releaseVersion is now available. You have a $appVersion
  # Extends to `recommended` and `available`, but not `required` ⬇️
  reminder_period_hours: 96
  progressive_gradual_rollout_hours: 96
  delay_hours: 20

  # # Skip
  # can_skip_release:
  #   required: false
  #   recommended: false
  #   available: true

  # # Later
  # can_postpone_release:
  #   required: false
  #   recommended: true
  #   available: true

  # Skip
  can_skip_release: true

  # Later
  can_postpone_release: true


settings_1:
  dialog:
    base:
      title: title
      description: description
      can_skip_release: true
    required:
      title: title
      description: description
      can_skip_release: true
    available:
      title: title
      description: description
      can_skip_release: true


settings_2:
  base:
    title: title
    description: description
    can_skip_release: true    
  required:
    title: title
    description: description
    can_skip_release: true
  available:
    title: title
    description: description
    can_skip_release: true

settings_3:
  base:
    can_skip_release: true
    update_button_text: Go to Store
  dialog:
    unsupported:
      title: title
      description: description
      can_skip_release: true
    updatable:
      title: title
      description: description
      can_skip_release: true
  card:
    title: title
    description: description
    can_skip_release: true