# Riverpod Utils

Утилиты и extension-методы для `riverpod`.

Пакет содержит:

- helper-методы для `AsyncNotifier`
- кеширование `autoDispose`-провайдеров через `ref.keepAlive()`
- утилиты для выборки части данных из `AsyncValue`
